{
  "name": "freelance-escrow-subgraph",
  "version": "0.0.1",
  "private": true,
  "description": "The Graph subgraph indexing FreelanceEscrow events on Sepolia.",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test",
    "deploy": "graph deploy"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2",
    "matchstick-as": "0.6.0"
  }
}
